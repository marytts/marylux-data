plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        flamlPlugin {
            id = 'flaml'
            implementationClass = 'FlamlPlugin'
        }
    }
}

repositories {
    jcenter()
}

dependencies {
    compile group: 'org.yaml', name: 'snakeyaml', version: '1.18'
}
